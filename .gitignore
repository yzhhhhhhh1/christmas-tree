# --- 操作系统文件 (不需要上传) ---
.DS_Store
Thumbs.db
Desktop.ini
._*

# --- 编辑器/IDE 配置 (个人设置不需要同步) ---
.vscode/
.idea/
*.swp
*.swo

# --- 依赖包 (如果您以后使用了 npm) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# --- 环境配置 (防止密钥泄露) ---
.env
.env.local
.env.*.local

# --- 临时文件 ---
dist/
build/
tmp/
temp/

# --- 项目特定说明 ---

# ⚠️ 注意：不要忽略 photos/ 文件夹！
# 因为 Vercel 需要里面的图片来展示默认相册。
# 如果您有不想上传的“测试图片”，建议单独放在一个叫 test_photos 的文件夹里
# 然后取消下面这行的注释：
# test_photos/

# 如果您有原始的高清视频素材不想传到 GitHub (GitHub 有单文件 100MB 限制)
# raw_video/